name: food_deals
description: "A Food Deals Platform."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  
  # Localization
  easy_localization: ^3.0.0

  # Datasource
  dio: ^4.0.4
  http: ^0.13.4
  shared_preferences: ^2.0.13
  agora_rtc_engine: ^5.3.0

  # Architecture libs
  provider: ^6.0.2
  dartz: ^0.10.1
  equatable: ^2.0.3
  get_it: ^7.2.0

  # Helper
  intl: ^0.19.0
  logger: ^1.1.0
  permission_handler: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # Testing
  mockito: ^5.3.2
  build_runner: ^2.3.3

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/translations/